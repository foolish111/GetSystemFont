#ifndef LINE_H
#define LINE_H

#include <QFontComboBox>

class line : public QFontComboBox
{
    Q_OBJECT
public:
    explicit line(QDeclarativeItem *parent = 0);

signals:

public slots:

};

#endif // LINE_H
